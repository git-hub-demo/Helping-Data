############## AJAX SIDE ############### 
$.each(result,function (index, value) {
                            $('#course1Id').append("<option value='" +
                                value.CourseId +
                                "'>" +
                                value.CourseName +
                                "</option>");
                                });
